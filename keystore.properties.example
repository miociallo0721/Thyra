# Copy to keystore.properties and fill in local values. Do not commit that file.
# Use forward slashes on Windows to avoid Properties escape handling.
storeFile=C:/Users/your-name/.android/thyra-upload.jks
storePassword=
keyAlias=thyra-upload
keyPassword=
